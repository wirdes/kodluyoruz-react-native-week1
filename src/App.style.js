import {StyleSheet} from 'react-native';

export default StyleSheet.create({
  safeAreaView: {
    flex: 1,
  },
  productText: {
    textAlign: 'center',
    fontSize: 55,
    fontWeight: 'bold',
  },
});
